I have learnt binary search after this linear search so let me write the method in java for both below:
1.for linear search:
  public boolean find(int key){
	int j;
	for(j=0;j<n;j++){
	      if(a[j]==key)
	         break;
		if(j==n)
		return false;
		else 
		true;


2.for binary search:
public boolean find(int key){
         int lb=0,ub=n-1;
				 while(lb<=ub){
				 int mid=(lb+ub)/2;
				      if(a[mid]==key)
							retrun true;
							if(a[mid]>key)
							ub=mid-1;
							if(a[mid]<key)
							lb=mid+1;
							
						}
				return false;
			}

	
